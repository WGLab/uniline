package uniline::Test;
use strict;
use warnings;
use uniline::Stats;

sub testScore {

die "calcGapScore has bug" unless &uniline::Stats::calcGapScore() != ;
die "calcGapScore has bug" unless &uniline::Stats::calcGapScore();
}
1;
